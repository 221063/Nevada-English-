class Config:
    DEBUG = True
    HOST = '0.0.0.0'
    PORT = 5000
    API_URL = '/analyze_emotion'

# Ejemplo de variables sensibles (reemplazar con valores reales en producción)
class Secrets:
    TEST_KEY = "test_secret_key"
